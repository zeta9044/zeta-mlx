[tool.poetry]
name = "zeta-mlx-cli"
version = "0.3.1"
description = "CLI for Zeta MLX Server"
authors = ["ZetaLab <zeta9044@gmail.com>"]
packages = [{include = "zeta_mlx", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10,<3.13"
zeta-mlx-core = "^0.3.1"
zeta-mlx-inference = "^0.3.1"
zeta-mlx-embedding = "^0.3.1"
typer = {extras = ["all"], version = "^0.16"}
rich = "^13.7"

[tool.poetry.scripts]
zeta-mlx = "zeta_mlx.cli:cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
